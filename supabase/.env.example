# Copy to supabase/.env (git-ignored) and fill in real values.
# Used by `supabase start` to resolve env(...) references in config.toml.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=
SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=
